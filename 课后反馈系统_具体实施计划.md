# 课后反馈系统 —— 具体实施计划

> 版本：v1.0  
> 日期：2026-08-11  
> 基于《课后反馈系统 —— 实现说明文档》整理  
> 第一阶段目标：完成“作业创建 → 学生提交 → OCR/结构化 → AI批改 → 教师复核 → 学情更新 → 课后反馈”的完整业务闭环。

---

## 1. 实施目标

第一版不追求一次性建设完整教育平台，而是优先验证以下三个核心问题：

1. AI 批改是否能有效降低教师重复批改工作量。
2. 学生作业数据是否可以持续沉淀为稳定的知识点学习记录。
3. 系统生成的学情分析和课后反馈是否真正对教师教学有价值。

第一版建议聚焦：

- 单学校 / 单机构
- Web 端
- 教师端 + 学生端 + 简单管理员端
- 单科优先，建议从初中数学开始
- 第三方 OCR
- API 大模型
- 模块化单体架构
- Docker Compose 部署

第一版暂不实现：

- 多校区 SaaS
- 家长端
- 微信小程序
- 自动组卷
- RAG
- 向量数据库
- BKT / IRT
- 自研 OCR
- 本地大模型
- 微服务拆分

---

# 2. 第一版业务闭环

整体流程如下：

```text
教师创建作业
        ↓
录入题目
        ↓
配置标准答案 / 评分规则
        ↓
绑定知识点
        ↓
发布作业
        ↓
学生提交
        ↓
文本 / 图片 / PDF
        ↓
OCR / 文本提取
        ↓
答案结构化
        ↓
批改路由器
        ↓
规则批改 / AI批改 / 混合批改
        ↓
生成结构化批改结果
        ↓
教师复核
        ↓
确认最终结果
        ↓
写入学生知识点学习记录
        ↓
计算知识点掌握度
        ↓
生成个人学情
        ↓
生成课后反馈
```

核心原则：

> AI 只负责辅助判断和生成建议，最终教学结果由教师确认。

---

# 3. 技术架构

## 3.1 推荐技术栈

### 前端

```text
Vue 3
Vite
Element Plus
Axios
Pinia
Vue Router
ECharts
```

### 后端

```text
Python
FastAPI
SQLAlchemy
Pydantic
Alembic
```

### 数据层

```text
PostgreSQL
Redis
```

### 异步任务

推荐：

```text
Celery + Redis
```

或者第一版简化为：

```text
RQ + Redis
```

### 文件存储

开发环境：

```text
MinIO
```

生产环境可切换：

```text
阿里云 OSS
AWS S3
腾讯云 COS
```

### AI

第一版建议通过统一接口接：

```text
DeepSeek
通义千问
OpenAI
```

业务代码不要直接绑定某一家模型。

### OCR

优先第三方：

```text
百度 OCR
阿里云 OCR
腾讯云 OCR
```

后续再考虑 PaddleOCR。

---

# 4. 第一版系统架构

```text
┌───────────────────────────┐
│           Vue3            │
│                           │
│ 教师端 / 学生端 / 管理端    │
└─────────────┬─────────────┘
              │
              │ HTTP
              ▼
┌───────────────────────────┐
│          FastAPI          │
│                           │
│ Auth                      │
│ Class                     │
│ Assignment                │
│ Question                  │
│ Submission                │
│ Grading                   │
│ Knowledge                 │
│ Analytics                 │
│ Feedback                  │
└───────┬─────────┬─────────┘
        │         │
        ▼         ▼
 PostgreSQL     Redis
                  │
                  ▼
             Task Queue
                  │
          ┌───────┴────────┐
          ▼                ▼
       OCR API          LLM API

文件：
        MinIO / OSS
```

第一版采用：

> 模块化单体架构。

不要一开始拆微服务。

当未来 AI 批改任务量明显增加后，再拆分：

```text
grading-worker
ocr-worker
analytics-worker
feedback-worker
```

---

# 5. 后端项目结构

建议目录：

```text
backend/
├── app/
│   ├── main.py
│   │
│   ├── core/
│   │   ├── config.py
│   │   ├── security.py
│   │   ├── database.py
│   │   └── redis.py
│   │
│   ├── auth/
│   ├── users/
│   ├── classes/
│   ├── assignments/
│   ├── questions/
│   ├── submissions/
│   ├── grading/
│   ├── knowledge/
│   ├── analytics/
│   ├── feedback/
│   ├── ai/
│   ├── ocr/
│   ├── storage/
│   └── tasks/
│
├── migrations/
├── tests/
├── Dockerfile
├── docker-compose.yml
├── requirements.txt
└── .env
```

---

# 6. 前端项目结构

```text
frontend/
├── src/
│   ├── api/
│   ├── components/
│   ├── layouts/
│   ├── router/
│   ├── stores/
│   ├── utils/
│   └── views/
│       ├── auth/
│       ├── teacher/
│       ├── student/
│       └── admin/
│
├── Dockerfile
└── package.json
```

教师端重点页面：

```text
登录
班级管理
学生管理
作业管理
题目编辑
作业提交列表
AI批改结果
教师复核
学生学情
班级学情
课后反馈
```

学生端重点页面：

```text
登录
我的作业
作业详情
提交作业
批改结果
错题
个人学情
课后反馈
```

---

# 7. 核心数据模型

第一版建议围绕：

```text
Question
+
KnowledgePoint
+
SubmissionAnswer
```

构建整个数据体系。

---

## 7.1 users

```text
id
username
password_hash
name
role
status
created_at
updated_at
```

role：

```text
admin
teacher
student
```

---

## 7.2 classes

```text
id
name
grade
teacher_id
created_at
updated_at
```

例如：

```text
初二3班
```

---

## 7.3 class_students

```text
class_id
student_id
joined_at
```

---

## 7.4 assignments

```text
id
class_id
teacher_id

title
subject
description

due_at
status

created_at
updated_at
```

status：

```text
draft
published
closed
archived
```

---

## 7.5 questions

这是第一版必须重点建设的表。

```text
id

subject
grade

question_type

content
standard_answer

score
difficulty

grading_rule JSONB

created_at
updated_at
```

question_type：

```text
single_choice
multiple_choice
judge
fill
calculation
short_answer
```

例如：

```text
题目：
解方程 x² - 5x + 6 = 0

question_type:
calculation

score:
10

standard_answer:
x = 2 或 x = 3
```

---

## 7.6 assignment_questions

```text
assignment_id
question_id
question_order
```

形成：

```text
Assignment
   │
   ├── Question 1
   ├── Question 2
   ├── Question 3
   └── Question 4
```

---

## 7.7 knowledge_points

```text
id

subject
grade
chapter

name
code

parent_id

created_at
```

例如：

```text
数学
└── 二次函数
    ├── 二次函数图像
    ├── 顶点
    ├── 对称轴
    └── 参数关系
```

---

## 7.8 question_knowledge_points

```text
question_id
knowledge_point_id
weight
```

例如：

```text
二次函数图像     0.7
函数单调性       0.3
```

知识点体系必须尽早确定。

否则后续：

```text
学情统计
薄弱点分析
错题归因
个性化推荐
```

都会受到影响。

---

## 7.9 submissions

```text
id

assignment_id
student_id

content_type
content_url

status

submitted_at
updated_at
```

content_type：

```text
text
image
pdf
```

status：

```text
submitted
processing
ai_graded
teacher_reviewed
completed
failed
```

---

## 7.10 submission_answers

用于记录每一道题的学生答案。

```text
id

submission_id
question_id

student_answer
ocr_text

is_correct

score
max_score

created_at
updated_at
```

关系：

```text
Submission
   │
   ├── SubmissionAnswer 1
   ├── SubmissionAnswer 2
   ├── SubmissionAnswer 3
   └── SubmissionAnswer 4
```

---

## 7.11 grading_results

```text
id

submission_answer_id

grading_type
model_name

ai_score
ai_comment

confidence

error_type
error_points JSONB

raw_ai_result JSONB

teacher_score
teacher_comment

status

created_at
reviewed_at
```

grading_type：

```text
rule
ai
hybrid
```

status：

```text
pending
ai_completed
manual_review
confirmed
```

AI 原始结果和教师最终结果必须同时保留。

---

## 7.12 student_knowledge_records

这张表保存原始学习轨迹。

```text
id

student_id
knowledge_point_id

question_id
assignment_id

is_correct

score
max_score

difficulty

error_type

answered_at
```

不要只存最终统计值。

原始数据必须长期保留，以便未来升级掌握度算法。

---

## 7.13 student_knowledge_stats

这是聚合结果表。

```text
student_id
knowledge_point_id

correct_count
wrong_count

mastery_score

trend

last_updated
```

第一版 mastery_score 可以先用：

```text
correct_count
--------------------------
correct_count + wrong_count
```

后面再增加：

```text
时间衰减
题目难度
BKT
IRT
```

---

# 8. AI 批改引擎设计

AI 批改不能简单设计成：

```text
答案
↓
DeepSeek
↓
结果
```

第一版应该实现：

```text
Grading Engine
```

---

## 8.1 Grading Router

```text
题目
  │
  ▼
QuestionType
  │
  ├── 选择题
  │      ↓
  │   RuleGrader
  │
  ├── 判断题
  │      ↓
  │   RuleGrader
  │
  ├── 填空题
  │      ↓
  │   HybridGrader
  │
  ├── 计算题
  │      ↓
  │   LLMGrader
  │
  └── 简答题
         ↓
      LLMGrader
```

---

# 9. Grading Engine 代码模块

建议：

```text
app/grading/
├── router.py
├── base.py
├── rule_grader.py
├── llm_grader.py
├── hybrid_grader.py
├── validator.py
├── prompts.py
└── schemas.py
```

伪代码：

```python
class GradingRouter:

    def grade(self, question, answer):

        if question.type in [
            "single_choice",
            "multiple_choice",
            "judge"
        ]:
            return RuleGrader.grade(
                question,
                answer
            )

        if question.type == "fill":
            return HybridGrader.grade(
                question,
                answer
            )

        return LLMGrader.grade(
            question,
            answer
        )
```

---

# 10. AI 输出 Schema

必须强制结构化输出。

推荐：

```json
{
  "score": 6,
  "max_score": 10,

  "is_correct": false,

  "confidence": 0.86,

  "error_type": "concept_error",

  "comment": "对二次函数顶点公式理解存在问题",

  "error_points": [
    {
      "position": "第二步",
      "description": "顶点横坐标公式使用错误"
    }
  ],

  "knowledge_points": [
    {
      "id": "kp_101",
      "name": "二次函数顶点",
      "mastery": "weak"
    }
  ]
}
```

后端使用：

```text
Pydantic
```

校验。

处理流程：

```text
AI 返回
   ↓
JSON Parse
   ↓
Pydantic Validate
   ↓
成功？
 ┌─┴─┐
是   否
│    │
落库 重试一次
     │
     ↓
   仍失败
     ↓
manual_review
```

---

# 11. 置信度策略

建议第一版：

```text
confidence >= 0.85
正常展示

0.70 <= confidence < 0.85
提示教师重点检查

confidence < 0.70
强制人工复核
```

但置信度不能作为“AI 一定正确”的依据。

后续需要通过：

```text
AI_score
vs
teacher_score
```

持续评估不同：

```text
题型
模型
Prompt
科目
```

下的真实批改准确率。

---

# 12. OCR 工作流

第一版：

```text
图片上传
   ↓
MinIO
   ↓
OCR Task
   ↓
OCR API
   ↓
raw_text
   ↓
答案切分
   ↓
SubmissionAnswer
```

第一阶段不要自己训练 OCR。

重点保证：

```text
图片上传稳定
OCR 可重试
OCR 原始结果可查看
教师可修正 OCR
```

---

# 13. 异步任务系统

AI 批改属于耗时任务，不能阻塞 HTTP 请求。

推荐：

```text
FastAPI
   │
   ▼
Redis
   │
   ▼
Celery Worker
```

任务类型：

```text
ocr_submission
grade_submission
update_student_stats
generate_feedback
```

完整链路：

```text
学生提交
   ↓
创建 submission
   ↓
返回 submission_id
   ↓
后台 OCR
   ↓
批改
   ↓
更新状态
   ↓
前端轮询 / WebSocket
   ↓
展示结果
```

第一版前端可以先：

```text
3~5 秒轮询一次
```

以后再换 WebSocket。

---

# 14. 教师复核模块

这是 MVP 的重点页面之一。

单题展示：

```text
学生：张三

第 3 题
────────────────────

题目：
xxxxxxxx

学生答案：
xxxxxxxx

标准答案：
xxxxxxxx

AI 批改：
6 / 10

错误类型：
概念理解错误

错误位置：
第二步

AI 评语：
xxxxxxxx

AI置信度：
86%

教师分数：
[ 7 ]

教师评语：
[                         ]

[确认]
```

操作：

```text
AI结果认可
修改分数
修改评语
重新AI批改
标记异常
上一题
下一题
```

作业级：

```text
确认全部批改
```

确认以后才进入正式学情统计。

---

# 15. 学情分析设计

第一版分析三个层级。

---

## 15.1 单次作业

包括：

```text
总分
正确率
错误题数量
薄弱知识点
主要错误类型
```

---

## 15.2 学生长期学情

包括：

```text
平均成绩
作业完成率
知识点掌握度
薄弱知识点 TOP N
进步知识点
退步知识点
重复错误
成绩趋势
```

---

## 15.3 班级学情

包括：

```text
平均分
成绩分布
知识点整体正确率
共性错误
薄弱知识点排行
未提交学生
```

---

# 16. 第一版掌握度计算

第一阶段先使用简单模型。

基础：

```text
mastery_score =
correct_count /
(correct_count + wrong_count)
```

可以加入简单时间权重：

```text
最近 7 天   权重 1.0
8~30 天     权重 0.8
31~90 天    权重 0.5
90 天以上   权重 0.3
```

后续再考虑：

```text
难度权重
错误类型
BKT
IRT
```

第一版不要过度复杂化。

---

# 17. Feedback Engine

课后反馈不要直接让 LLM 读取全部原始作业。

正确设计：

```text
student_knowledge_records
        ↓
Analytics Engine
        ↓
结构化结果
        ↓
Feedback Engine
        ↓
LLM
        ↓
自然语言报告
```

输入：

```json
{
  "student": "张三",
  "assignment_score": 82,

  "strong_topics": [
    "一元二次方程"
  ],

  "weak_topics": [
    {
      "topic": "二次函数",
      "mastery": 0.48,
      "trend": "down"
    }
  ],

  "repeated_errors": [
    {
      "topic": "二次函数顶点",
      "times": 3
    }
  ]
}
```

Prompt：

```text
你是一名教师助手。

根据以下真实结构化学习数据，
生成一份 300 字以内的课后反馈。

要求：

1. 不得编造数据。
2. 首先说明本次表现。
3. 指出 1~3 个主要薄弱知识点。
4. 指出是否存在重复错误。
5. 给出下一步学习建议。
6. 语言客观、清晰。
```

---

# 18. 核心 API 设计

## Auth

```http
POST /api/auth/login
GET  /api/auth/me
```

---

## Classes

```http
POST /api/classes
GET  /api/classes
GET  /api/classes/{id}

POST /api/classes/{id}/students
GET  /api/classes/{id}/students
```

---

## Assignments

```http
POST /api/assignments
GET  /api/assignments
GET  /api/assignments/{id}

PUT  /api/assignments/{id}
POST /api/assignments/{id}/publish
```

---

## Questions

```http
POST /api/questions
GET  /api/questions/{id}
PUT  /api/questions/{id}

POST /api/assignments/{id}/questions
```

---

## Submission

```http
POST /api/assignments/{id}/submit

GET /api/submissions/{id}

GET /api/assignments/{id}/submissions
```

---

## Grading

```http
POST /api/submissions/{id}/grade

GET /api/submissions/{id}/grading

PUT /api/gradings/{id}/confirm

POST /api/gradings/{id}/retry
```

---

## Student Analytics

```http
GET /api/students/{id}/knowledge-stats

GET /api/students/{id}/learning-trend

GET /api/students/{id}/weak-points
```

---

## Class Analytics

```http
GET /api/classes/{id}/analytics

GET /api/classes/{id}/weak-points
```

---

## Feedback

```http
GET /api/students/{id}/feedback

GET /api/students/{id}/feedback-report?period=week

POST /api/students/{id}/feedback/generate
```

---

# 19. 六周开发计划

---

# 第 1 周：基础框架 + 数据模型

## 目标

完成：

```text
登录
班级
学生
作业
题目
知识点
```

基础能力。

## 后端

完成：

```text
FastAPI 项目初始化
SQLAlchemy
Alembic

PostgreSQL
Redis
MinIO

JWT
RBAC
```

创建：

```text
users
classes
class_students
assignments
questions
assignment_questions
knowledge_points
question_knowledge_points
```

## 前端

完成：

```text
登录页
教师布局
班级管理
学生管理
作业列表
作业创建
题目编辑
```

## 第 1 周验收标准

教师可以：

```text
登录
↓
创建班级
↓
添加学生
↓
创建作业
↓
添加题目
↓
给题目绑定知识点
↓
发布作业
```

---

# 第 2 周：学生提交模块

## 目标

跑通：

```text
学生登录
↓
查看作业
↓
提交答案
```

## 开发内容

完成：

```text
学生作业列表
作业详情
文本提交
图片上传
PDF上传
MinIO存储
```

数据库：

```text
submissions
submission_answers
```

## 验收

学生能够：

```text
登录
↓
找到作业
↓
提交一份图片作业
↓
服务端保存成功
↓
教师端可以看到提交记录
```

---

# 第 3 周：OCR + AI 批改引擎

## 目标

实现第一版自动批改。

## 开发内容

接入：

```text
OCR
LLM
Redis
Celery
```

实现：

```text
GradingRouter
RuleGrader
HybridGrader
LLMGrader
```

实现结构化输出。

建立：

```text
grading_results
```

## 验收标准

一份 10 道题的作业提交后，系统可以自动生成：

```text
每题分数
总分
正确 / 错误
错误类型
错误位置
知识点
AI评语
confidence
```

---

# 第 4 周：教师复核系统

## 目标

AI 批改进入真实教师工作流。

## 开发内容

教师端：

```text
待批改列表
AI 已批改
已确认
```

单题：

```text
题目
学生答案
标准答案
AI分数
AI评语
错误点
置信度
```

教师可以：

```text
修改分数
修改评语
确认结果
```

## 验收标准

教师能够完整复核一名学生的整份作业。

确认后：

```text
grading_result.status = confirmed
```

并写入：

```text
student_knowledge_records
```

---

# 第 5 周：学情分析

## 目标

让作业数据开始形成学习画像。

## 开发内容

完成：

```text
student_knowledge_stats
```

计算：

```text
知识点正确率
掌握度
薄弱点
趋势
```

教师端展示：

```text
学生成绩趋势
知识点掌握度
薄弱知识点
重复错误
```

班级端：

```text
平均分
知识点正确率
共性错误
```

前端图表：

```text
ECharts
```

## 验收标准

连续提交至少 3 次作业后，可以看到：

```text
学生知识点趋势
薄弱知识点排行
班级共性错误
```

---

# 第 6 周：课后反馈 + 测试 + 部署

## 目标

完成 MVP 闭环。

## 开发内容

实现：

```text
Feedback Engine
```

生成：

```text
单次作业反馈
学生周报
```

完成：

```text
异常处理
权限检查
日志
模型超时
任务重试
AI JSON解析失败处理
OCR失败处理
```

部署：

```text
Docker Compose
Nginx
HTTPS
```

---

# 20. 第 6 周真实测试

建议找：

```text
1 名教师
10~20 名学生
3~5 次真实作业
```

测试。

重点收集：

### 教师

```text
AI批改准确率
教师修改比例
批改时间节省比例
最常修改什么
哪些AI评语无价值
教师愿不愿继续使用
```

### 学生

```text
反馈是否看得懂
错误定位是否准确
学习建议是否有帮助
```

---

# 21. 第一阶段重点指标

不要只看系统是否“能运行”。

需要记录：

## AI 修改率

```text
教师修改过的AI结果
-----------------------
全部AI结果
```

目标：

```text
越低越好
```

---

## AI 与教师分数偏差

```text
abs(
AI_score - teacher_score
)
```

分别统计：

```text
选择题
填空题
计算题
简答题
```

---

## 教师批改时间

例如：

```text
传统批改：
30分钟

AI辅助：
12分钟
```

节省：

```text
60%
```

这是产品最重要的价值指标之一。

---

# 22. Prompt 优化机制

建立：

```text
grading_prompt_versions
```

至少记录：

```text
version
prompt
model
created_at
```

每次批改：

```text
grading_result
```

关联：

```text
prompt_version
```

以后分析：

```text
Prompt V1
教师修改率 28%

Prompt V2
教师修改率 17%
```

通过真实数据优化，而不是凭感觉改 Prompt。

---

# 23. 日志与可观测性

至少记录：

```text
OCR调用
LLM调用
token使用
调用耗时
模型
任务失败
任务重试
教师修改
```

推荐：

```text
logs/
```

开发期先结构化日志。

后续可以上：

```text
Prometheus
Grafana
Sentry
```

第一版不强制。

---

# 24. AI 成本控制

第一版必须控制成本。

策略：

### 客观题不调用 LLM

```text
选择
判断
部分填空
```

直接规则批改。

### 相同答案缓存

完全一样的：

```text
题目 + 答案
```

可以复用结果。

### 设置模型超时

例如：

```text
30 秒
```

### 控制 Token

不要给模型传：

```text
整个班全部历史作业
```

只传：

```text
当前题目
标准答案
评分规则
学生答案
必要上下文
```

---

# 25. 数据安全

学生数据至少按照以下原则处理：

```text
姓名
成绩
班级
作业
```

与第三方 AI 调用解耦。

发给 AI：

```text
student_id: S001
```

而不是：

```text
张三
某某学校
初二3班
```

做到：

```text
业务数据库保存真实身份

AI请求使用匿名ID
```

并保留：

```text
AI原始建议
教师最终决定
操作日志
```

---

# 26. MVP 完成标准

第一阶段不是“写完代码”就算完成。

至少满足以下业务闭环：

```text
教师登录
↓
创建班级
↓
添加学生
↓
创建作业
↓
录入题目
↓
绑定知识点
↓
发布
↓
学生登录
↓
提交作业
↓
OCR
↓
AI自动批改
↓
教师复核
↓
确认
↓
更新学习记录
↓
更新知识点掌握度
↓
生成课后反馈
↓
教师查看班级学情
↓
学生查看个人反馈
```

这一整套跑通以后，第一版 MVP 才算真正完成。

---

# 27. 二期规划

第一版稳定后再进入二期。

建议按下面顺序扩展。

## P1

```text
错题本
```

根据：

```text
SubmissionAnswer
+
KnowledgePoint
```

自动形成错题。

---

## P2

```text
专项练习
```

根据：

```text
薄弱知识点
```

推荐：

```text
5~10 道题
```

---

## P3

```text
自动组卷
```

按照：

```text
知识点
难度
学生掌握度
```

生成个性化练习。

---

## P4

```text
周期学习报告
```

支持：

```text
周报
月报
学期报告
```

---

## P5

```text
家长端
```

查看：

```text
作业完成情况
知识点掌握
成长趋势
学习建议
```

---

## P6

升级知识追踪：

```text
BKT
IRT
```

---

# 28. 长期产品路线

最终可以分三层。

## 第一层：AI 批改助手

核心价值：

```text
减少教师重复批改时间
```

---

## 第二层：AI 学情分析系统

核心价值：

```text
知道每个学生
哪里会
哪里不会
为什么错
有没有进步
```

---

## 第三层：AI 个性化教学系统

完整闭环：

```text
作业
↓
批改
↓
错题
↓
知识点
↓
学习画像
↓
专项练习
↓
再次作答
↓
重新评估
↓
更新掌握度
```

形成：

```text
学
↓
练
↓
测
↓
评
↓
分析
↓
再练
```

这是这个项目长期真正值得发展的方向。

---

# 29. 开发优先级总结

## P0 —— 必须完成

```text
登录权限
班级学生
作业管理
Question
KnowledgePoint
Submission
OCR
Grading Engine
教师复核
student_knowledge_records
基础学情
课后反馈
```

## P1 —— MVP 后立即补

```text
错题本
Prompt版本管理
模型效果统计
批量批改
更好的图片批改界面
```

## P2 —— 第二阶段

```text
专项练习
自动组卷
知识追踪
家长端
周期报告
```

## P3 —— 规模化之后

```text
多租户
多校区
微服务
私有化模型
模型评测平台
高级知识追踪模型
```

---

# 30. 最终实施原则

整个项目开发过程中建议坚持以下原则：

1. **先闭环，再扩功能。**
2. **先保证教师可控，再提高 AI 自动化程度。**
3. **原始学习数据必须保留。**
4. **Question 和 KnowledgePoint 是核心数据资产。**
5. **客观题优先规则，主观题才使用大模型。**
6. **AI 输出必须结构化。**
7. **所有 AI 结果都应允许教师复核。**
8. **学情计算依赖结构化数据，不直接依赖 LLM。**
9. **LLM 主要负责语义判断和自然语言生成。**
10. **第一版重点验证教师是否愿意真实使用。**

第一阶段真正需要做好的，不是功能数量，而是下面这一条链：

```text
作业
→ 答题
→ 批改
→ 复核
→ 知识点
→ 学情
→ 反馈
```

只要这一条链稳定跑通，后续错题本、个性化练习、自动组卷、家长报告和智能教学都可以基于现有数据继续扩展。
